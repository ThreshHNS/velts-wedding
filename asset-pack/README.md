# Wedding game asset pack

This pack contains first reference slices from the generated pixel-art wedding game mockup, plus implementation docs and source wedding data.

Important: files in `reference_slices/` are not production-ready transparent sprites. They are layout/style references. Use `docs/codex_image2sprite_prompt.md` to regenerate production sprites with image2sprite.
